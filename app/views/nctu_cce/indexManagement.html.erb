<%= render partial: "header", locals: {item: @item} %>
<div class="container-fluid">
	<div class="row">	
		<ul class="nav nav-tabs col-md-3">
		  <li role="presentation" class="active"><a href="#">報名學員表</a></li>
		  <li role="presentation"><a href="#">匯款入帳表</a></li>
		</ul>
		<ul class="nav nav-pills col-md-offset-6  col-md-3">
		  <li role="presentation">
		  	<%=link_to '名稱簡介設定', {controller: 'nctu_cce', action: 'editGroup', id: @item.group.id} %>
		  </li>
		  <li role="presentation">
			<%=link_to '基本資料設定', {controller: 'nctu_cce', action: 'editItem', id: @item.id} %>		  	
		  </li>
		</ul>		
	</div>
</div>
<br>
<table class="table table-striped table-hover ">
  <tr>
    <th>姓名</th>
    <th>聯絡電話</th>
    <th>email</th>
    <th>報名進度</th>        
  </tr>	
<%@progresses.each do |p|%>
<tr id='link1' onclick="document.location='<%=host%>/nctu_cce/showProgress?id=<%=p.id%>';">
    <td><%=p.user.name%></td>
    <td><%=p.user.phone_no%></td>
    <td><%=p.user.email%></td>
    <td><%=p.stage%></td>
</tr>
<%end%>
</table>

<div class="pagination">
  <%= will_paginate @progresses, renderer: BootstrapPagination::Rails %>
</div>