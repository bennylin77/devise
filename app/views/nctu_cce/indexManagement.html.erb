
  <div class="page-header">
    <h1><%= @item.group.title%></h1>      
  </div>
  
<div class="container-fluid">
	<div class="row">	
		<ul class="nav nav-pills  col-md-3">
		  <li role="presentation" class="active"><a href="#">報名學員表</a></li>
		  <li role="presentation"><a href="#">匯款入帳表</a></li>
		</ul>
		<div class="col-md-offset-5  col-md-2">
		<%=link_to '名稱簡介設定', {controller: 'nctu_cce', action: 'editGroup', id: @item.group.id}, {class: "btn btn-info", role: "button"} %>
		</div>		
		<div class="col-md-2">
		<%=link_to '基本資料設定', {controller: 'nctu_cce', action: 'editItem', id: @item.id}, {class: "btn btn-info", role: "button"} %>
		</div>
	</div>
</div>

<table class="table table-striped table-hover ">
<%@progresses.each do |p|%>
<tr id='link1' onclick="document.location='<%=host%>/nctu_cce/showProgress?id=<%=p.id%>';">
    <td><%=p.user.name%></td>
    <td><%=p.user.phone_no%></td>
    <td><%=p.user.email%></td>
    <td><%=p.stage%></td>
</tr>
<%end%>
</table>

<div class="pagination">
  <%= will_paginate @progresses, renderer: BootstrapPagination::Rails %>
</div>